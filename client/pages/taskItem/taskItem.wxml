<!--作业列表-->
<view class="task-item__page">
  <!--页眉-->
  <view class="task-item__hd">
    <view class="task-item__title">{{taskBlock.BlockName}} {{taskBlock.FolderName}}</view>
    <view class="task-item__desc"></view>
  </view>

  <!--一份作业-->
  <view class="task-item__bd">

    <!--课程面板-->
    <block wx:for="{{item4Course}}">

      <!--课程栏目头部-->
      <view class="weui-cells__title">{{item.CourseName}}</view>

      <!--作业列表区-->
      <view class="weui-cells weui-cells_after-title">

        <view class="weui-cell" data-itemid="{{item.id}}" data-courseid="{{item.course_id}}" wx:for="{{item.taskItems}}" bindlongpress='onEditTaskItems'>

          <!--作业内容-->
          <view class="weui-cell__hd"></view>
          <view class="weui-cell__bd">{{index+1}}. {{item.ItemTitle}}</view>

          <!--作业条目菜单区-->
          <view class="weui-cell__ft" wx:if="{{true}}">
            <icon class="weui-icon-radio" type="waiting" size="20" data-itemid="{{item.id}}" data-courseid="{{item.course_id}}" bindtap="goToTaskDetail" hidden="{{item.canRemove}}"></icon>
            <view class="weui-badge remove_item" data-itemid="{{item.id}}" data-courseid="{{item.course_id}}" bindtap="onRemoveTaskItem" hidden="{{item.canRemove == false}}">x</view>
          </view>
        </view>

        <!--课程栏目底部-->
        <view class="weui-cell weui-cell_link">
          <view class="weui-cell__bd">
            <input placeholder=" + 添加作业" id="{{item.id}}" bindblur="doAddNewTaskItem" value="{{addNewTaskPromotion}}" />
          </view>
        </view>

      </view>

    </block>
  </view>

  <!--页脚-->
  <view class="task-item__ft"></view>
</view>